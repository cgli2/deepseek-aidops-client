# SQL 变更审查

version: 1.0

数据库结构变更（DDL）前的审查技能包示例，与 release-checklist 一起演示文件夹批量导入。

## 触发边界

修改数据库表结构、添加索引、变更字段，或需要审查 SQL 变更脚本

## 执行步骤

- 列出本次全部 DDL 变更与影响表
- 检查字段类型、默认值与空值约束兼容性
- 评估锁表/数据回填风险，给出在线变更建议
- 输出审查结论（可执行 / 需修改 / 拒绝）

## 验证规则

- 每项变更都有影响面结论
- 高风险变更附在线执行方案
